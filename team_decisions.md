[2026-02-14T23:21:17Z] DECISION v1 | runId=cursor_run_backend_subset_2026-02-14 | decision="Escalate - backend typecheck cannot be made deterministic under frozen-lockfile" | alternatives="A) Proceed with non-frozen install (violates determinism/ci-safe). B) Update pnpm-lock.yaml to include pg/@types/pg (ci_deploy trigger; requires approval flow). C) Remove pg/@types/pg from backend/package.json (behavioral risk; not failure-driven)." | rationale="Frozen install is the deterministic baseline; it fails because lockfile is stale vs backend/package.json. Without updating lockfile, backend typecheck failures can't be attributed to our dominance changes deterministically." | risks="Any lockfile change triggers ci_deploy approval; proceeding without frozen lockfile risks CI mismatch and non-reproducible builds." | rollback="No code fix commits created in this iteration. If needed: revert only memory logs and keep repo state unchanged; do not commit lockfile changes."
[2026-02-14T23:25:17Z] DECISION v1 | runId=cursor_run_backend_subset_2026-02-14 | decision="Approval Request (infra-unblock): sync root pnpm-lock.yaml for backend pg deps" | alternatives="A) Keep lockfile as-is (backend golden subset cannot be executed deterministically). B) Run non-frozen installs locally (non-ci-safe, non-deterministic). C) Sync pnpm-lock.yaml only (requires ci_deploy approval)." | rationale="`pnpm -C backend install --frozen-lockfile` fails deterministically with ERR_PNPM_OUTDATED_LOCKFILE because root pnpm-lock.yaml is out-of-sync with backend/package.json (missing specifiers: pg@^8.13.3, @types/pg@^8.11.10). This blocks deterministic backend subset (typecheck + unit)." | risks="Any pnpm-lock.yaml change triggers ci_deploy approval; must keep scope lockfile-only (no package.json changes) and immediately prove frozen install green after sync." | rollback="Revert the lockfile sync commit to restore previous lockfile state."
